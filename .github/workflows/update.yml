name: Update README

on:
  schedule:
    - cron: "0 * * * *"   # 每小时更新一次
  workflow_dispatch:       # 支持手动触发

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Run update script
        run: python update.py

      - name: Commit and push changes
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          git config --global user.name 'MonsterTower'
          git config --global user.email '1394869809@qq.com'
          git add README.md
          git commit -m "auto update" || echo "No changes"
          git push https://$PAT_TOKEN@github.com/MonsterTower/MonsterTower.git HEAD:main
